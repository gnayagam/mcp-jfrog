apiVersion: v1
kind: Secret
metadata:
  name: mcp-jfrog-secrets
  labels:
    app.kubernetes.io/name: mcp-jfrog
    app.kubernetes.io/component: server
type: Opaque
stringData:
  # Optional fallback only. Do not set when enforcing per-developer identity.
  jfrog_access_token: ""
